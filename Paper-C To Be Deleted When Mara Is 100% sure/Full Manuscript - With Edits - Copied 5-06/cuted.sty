% Local minimal fallback for sn-jnl when cuted is unavailable.
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{cuted}[2026/05/02 local minimal fallback]
\newenvironment{strip}{\par\begingroup}{\par\endgroup}
