% Local minimal fallback for sn-jnl when wrapfig is unavailable.
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{wrapfig}[2026/05/02 local minimal fallback]
\newenvironment{wrapfigure}[4][]{\begin{figure}}{\end{figure}}
\newenvironment{wraptable}[4][]{\begin{table}}{\end{table}}
