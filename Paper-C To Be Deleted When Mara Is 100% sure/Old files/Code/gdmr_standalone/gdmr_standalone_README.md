# gDMR standalone: article-style Bermudan put scripts

This folder contains a fully standalone Bermudan put setup under the generalized Gatheral double mean-reverting (gDMR) model.

The goal is to mirror the article-style workflow used in `heston_paper_benchmark`, but for the gDMR model from the manuscript:

- `run_gdmr_lsmc_put.py`
  Standalone Longstaff-Schwartz Monte Carlo with direct and low estimators.
- `run_gdmr_hybrid_put.py`
  Standalone hybrid LSMC-PDE with article-style pathwise pre-surfaces, volatility regression, and an out-of-sample low estimator.
- `compare_gdmr_put_prices.py`
  Runs both scripts, treats `LSMC direct` as the headline reference, prints relative errors, and saves a professional SVG comparison plot.

## Default model and option setup

- model = generalized Gatheral double mean-reverting (gDMR)
- option = Bermudan put
- `S0 = 100`
- `K = 100`
- `T = 1`
- `N_ex = 100`
- `M = 600`
- internal steps per exercise interval = `6`

## Default Monte Carlo setup

- `N = 1000000`
- basis degree = `3`
- basis size = `16`
- seed = `2026`
- low-seed = `2103`

## Default hybrid setup

- `N = 30000`
- `N_low = 30000`
- `N_S = 181`
- asset low factor = `0.35`
- asset high factor = `3.00`
- `N_hermite = 64`
- volatility basis degree = `3`
- volatility basis size = `10`
- volatility truncation quantile = `0.995`
- seed = `2026`
- low-seed = `2103`

## Main improvements

- the LSMC script now uses a richer degree-3 paper-style state basis with payoff powers up to degree `3`
- the default reference run is now a stronger `1000000`-path, `600`-step LSMC setup
- the hybrid script now uses a bounded, compact-support volatility basis on a truncated rectangle `D_v`, following the manuscript
- the hybrid asset grid was tightened around the strike so the direct estimator has better resolution near the exercise boundary
- the comparison script now uses `LSMC direct` as the reference value and reports clear relative errors and standard errors
- the comparison plot now shows point estimates with error bars instead of a simple gap-only bar chart

## Final verified default result

From the current default settings:

- LSMC direct = `6.384535`
- LSMC low = `6.387249`
- Hybrid direct = `6.421758`
- Hybrid low = `6.265070`
- Hybrid direct relative error vs LSMC direct = `0.58%`
- Hybrid low relative error vs LSMC direct = `1.87%`
- Hybrid low relative error vs LSMC low = `1.91%`

So the current default standalone `gdmr_standalone` run meets the `1%` direct-error target for `Hybrid direct`.

## Files generated by the comparison

- `compare_gdmr_put_prices.svg`
  Professional comparison chart with error bars and the `LSMC direct` reference line.
- `gdmr_results_and_improvements.md`
  Short record of the tuning path and the final improvements.
- `gdmr_code_explanation.md`
  Short formula-based note explaining the gDMR code and the final working result.
